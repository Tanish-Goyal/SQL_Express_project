<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            background-image:url("https://img.freepik.com/premium-vector/overlapping-forms-background_23-2148676581.jpg?w=2000");
        background-size: cover;

        }
        .container{
            border:2px solid rgb(94, 91, 91);
            border-radius: 1.5rem;
            background-color:rgba(0,0,0,0.3);
            margin:auto auto;
            width:25vw;
            margin-top:30vh;
            text-align: center;
            font-size: 30px;
            color:white;
            padding-bottom: 40px;
        }
        button{
                 box-shadow: 0px 4px 20px 0px #49c628a6;
                background-image: linear-gradient(135deg, #70F570 10%, #49C628 100%);
                padding: 12px 35px;
                color: #fff;
                font-size:16px;
                border-radius:1.5rem;
        }
        .inputs {
            overflow: hidden;
            }
        input{
            width: 80%;
            padding: 10px;
            margin-top: 25px;
            font-size: 16px;
            border: none;
            outline: none;
            border-bottom: 2px solid #B0B3B9;
        }
        a{
            color:white;
        }
    </style>

</head>
<body>
    <form action="/validate" method="post">
        <div class="container inputs">
        <table>
            <tr>
                <td><span>USERNAME</span><input type="text" name="uuname" placeholder="Enter username" minlength="2" maxlength="16" required></td>
            </tr>
            <td><span>PASSWORD</span><input type="password" name="ppass" placeholder="Enter password" minlength="8" maxlength="15" pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#$%^&*_=+-]).{8,12}$" required></td>

        </table>
    <br>

        <button type="submit">Submit</button>
    </div>
    </form>
</body>
</html>